import { KeyRound, MoreHorizontal, Plus, ShieldCheck, UserCheck } from "lucide-react";
import { Badge, Button, Page, PageHeader, Panel } from "../../design-system/react";

const staff = [{ name: "Jordan Cruz", username: "gerente", role: "Gerente", active: true, last: "Ahora" }, { name: "Ana López", username: "ana", role: "Barista", active: true, last: "Hoy, 10:24" }, { name: "Mario Díaz", username: "mario", role: "Barista", active: false, last: "8 ago, 18:12" }];
export function PeoplePage() { return <Page size="wide"><PageHeader eyebrow="ACCESOS INTERNOS" title="Personal" description="Usuarios únicos con PIN personal. No existe registro público." action={<Button variant="primary"><Plus size={18} /> Crear acceso</Button>} /><div className="grid gap-4 sm:grid-cols-3"><Panel className="flex items-center gap-4 p-5"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-primary-fixed text-primary"><UserCheck /></span><div><p className="text-sm text-on-surface-variant">Personal activo</p><p className="text-2xl font-bold">2</p></div></Panel><Panel className="flex items-center gap-4 p-5"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-tertiary-fixed text-tertiary"><ShieldCheck /></span><div><p className="text-sm text-on-surface-variant">Gerentes</p><p className="text-2xl font-bold">1</p></div></Panel><Panel className="flex items-center gap-4 p-5"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-secondary-container"><KeyRound /></span><div><p className="text-sm text-on-surface-variant">PIN restablecido</p><p className="text-2xl font-bold">0</p></div></Panel></div><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{staff.map((person) => <Panel key={person.username} className="p-5"><div className="flex items-start justify-between"><span className="flex h-12 w-12 items-center justify-center rounded-2xl bg-primary-fixed text-lg font-bold text-primary">{person.name.split(" ").map((word) => word[0]).slice(0, 2).join("")}</span><Button size="icon" variant="ghost"><MoreHorizontal /></Button></div><h2 className="mt-4 font-bold">{person.name}</h2><p className="text-sm text-on-surface-variant">@{person.username}</p><div className="mt-4 flex items-center justify-between"><Badge tone={person.role === "Gerente" ? "primary" : "neutral"}>{person.role}</Badge><Badge tone={person.active ? "success" : "danger"}>{person.active ? "Activo" : "Desactivado"}</Badge></div><div className="mt-4 border-t border-outline-variant/25 pt-4 text-xs text-on-surface-variant">Último acceso: {person.last}</div></Panel>)}</div></Page>; }
